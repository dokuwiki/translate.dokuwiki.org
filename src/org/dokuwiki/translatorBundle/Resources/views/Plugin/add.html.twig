{% extends 'dokuwikiTranslatorBundle:Default:index.html.twig' %}

{% block content %}
<ul class="breadcrumb">
    <li><a href="{{ path('dokuwiki_translator_homepage') }}">Home</a> <span class="divider">/</span></li>
    <li class="active"><a href="#">Add a Plugin</a></li>
</ul>
<div class="container">
    <div class="row">
        <div class="span12">
            <div class="page-header">
                <h1>Plugin Translation</h1>
            </div>
        </div>
        <div class="span4">
            <p>
                The DokuWiki translation tool allows you to add your DokuWiki 
                plugins to be translated by end users.
                Everybody will be able to use this tool and create a translation
                of your plugin.
                After adding your plugin you will receive translations as git patch via email or as
                <a href="https://help.github.com/articles/using-pull-requests" target="_blank">GitHub pull request</a>.
            </p>
        </div>
        <div class="span4">
            <p>
                To use this tool your plugin must be
            </p>
            <ul>
                <li>a public Git repository. The repository url must start with <em>ssh://</em>,
                    <em>http://</em> <em>https://</em> or <em>git://</em></li>
                <li>listed on the <a href="https://www.dokuwiki.org/plugins">plugins list</a></li>
                <li>
                    use the <a href="https://www.dokuwiki.org/devel:localization">DokuWiki localization functions</a>
                </li>
            </ul>
            <p>
                The Plugin name is the name of your plugin folder. It is always lower cased and has no special chars.
            </p>
        </div>
        <div class="span4">
            <p>
                There are some limitations to the language files. It isn't allowed to use nested arrays in the $lang array except for the js sub array.
            </p>
            <p>
                Additionally comments will be removed by the translation tool.
            </p>
        </div>

        <div class="span8 offset2">
            {% if errors is defined %}
                <ul>
                {% for error in errors %}
                    <li>{{ error.message }}</li>
                {% endfor %}
                </ul>
            {% endif %}
            <form action="{{ path('dokuwiki_translator_plugin_add') }}" class="form-horizontal" method="post">
                {{ form_widget(form) }}

                <div class="controls">
                    <input type="submit" class="btn btn-primary" value="Add my plugin!" />
                    <a href="{{ path('dokuwiki_translator_homepage') }}" class="btn">
                        Abort
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>

{% endblock %}
